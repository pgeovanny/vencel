import { register } from '../actions';
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="shell"><section className="card center"><div className="ey">NOVO ACESSO</div><h1>Criar conta</h1><p className="muted">Sua conta entra com o trial configurado no servidor. Nenhum privilégio administrativo é concedido pelo cadastro público.</p>{p.error&&<p className="error">Confira os dados ou use outro e-mail.</p>}<form action={register}><div className="field"><label>NOME</label><input name="name" required/></div><div className="field"><label>E-MAIL</label><input name="email" type="email" required/></div><div className="field"><label>SENHA</label><input name="password" type="password" minLength={8} required/></div><div className="field"><label>CONFIRMAR</label><input name="confirm" type="password" minLength={8} required/></div><button className="btn primary">Criar conta</button></form></section></main>}

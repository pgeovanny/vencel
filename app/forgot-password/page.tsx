'use client';import { requestReset } from '../actions';import { useSearchParams } from 'next/navigation';export default function Forgot(){const p=useSearchParams();return <main className="shell"><section className="card center"><div className="ey">RECUPERAÇÃO</div><h1>Nova senha</h1><p className="muted">Informe seu e-mail. O link retorna para uma rota dedicada e nunca entra direto na campanha.</p>{p.get('sent')==='1'&&<p className="ok">Link enviado. Abra seu e-mail.</p>}<form action={requestReset}><input type="hidden" name="origin" value={typeof window==='undefined'?'':window.location.origin}/><div className="field"><label>E-MAIL</label><input name="email" type="email" required/></div><button className="btn primary">Enviar link</button></form></section></main>}
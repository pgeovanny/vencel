import { login } from './actions';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="shell"><section className="card center"><div className="ey">ACESSO SEGURO</div><h1>JurisQuest</h1><p className="muted">Autenticação por cookies SSR, validação server-side e RLS no banco.</p>{p.error&&<p className="error">{p.error}</p>}<form action={login}><div className="field"><label>E-MAIL</label><input name="email" type="email" required autoComplete="email"/></div><div className="field"><label>SENHA</label><input name="password" type="password" required autoComplete="current-password"/></div><div className="row"><button className="btn primary">Entrar</button><a className="btn" href="/signup">Criar conta</a><a className="btn" href="/forgot-password">Recuperar senha</a></div></form></section></main>}

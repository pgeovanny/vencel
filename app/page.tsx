import { login } from './actions';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="shell"><section className="card center"><div className="ey">SUA CAMPANHA CONTINUA AQUI</div><h1>JurisQuest</h1><p className="muted">Estude o seu edital dentro de casos, decisões e revisões que mostram exatamente o que você domina e o que precisa recuperar.</p>{p.error&&<p className="error">{p.error}</p>}<form action={login}><div className="field"><label>E-MAIL</label><input name="email" type="email" required autoComplete="email"/></div><div className="field"><label>SENHA</label><input name="password" type="password" required autoComplete="current-password"/></div><div className="row"><button className="btn primary">Entrar na campanha</button><a className="btn" href="/signup">Criar conta</a><a className="btn" href="/forgot-password">Esqueci a senha</a></div></form><p className="muted" style={{fontSize:10,marginTop:16}}>Seu progresso, histórico de decisões e revisões ficam vinculados à sua conta.</p></section></main>}

import { redirect } from 'next/navigation';import { createClient } from '@/lib/supabase/server';import { updatePassword } from '../actions';export default async function Reset({searchParams}:{searchParams:Promise<{error?:string}>}){const sb=await createClient();const{data:{user}}=await sb.auth.getUser();if(!user)redirect('/');const p=await searchParams;return <main className="shell"><section className="card center"><div className="ey">CONTA VERIFICADA</div><h1>Definir nova senha</h1>{p.error&&<p className="error">Confira as senhas. Use pelo menos 8 caracteres.</p>}<form action={updatePassword}><div className="field"><label>NOVA SENHA</label><input name="password" type="password" minLength={8} required/></div><div className="field"><label>CONFIRMAR</label><input name="confirm" type="password" minLength={8} required/></div><button className="btn primary">Salvar nova senha</button></form></section></main>}